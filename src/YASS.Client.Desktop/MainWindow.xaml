<ui:FluentWindow x:Class="YASS.Client.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:YASS.Client.Desktop"
        xmlns:vm="clr-namespace:YASS.Client.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="YASS - Yet Another Screen Sharing" 
        Height="500" Width="500"
        MinHeight="400" MinWidth="400"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <ui:TitleBar Grid.Row="0" Title="YASS - Yet Another Screen Sharing">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="ShareScreenStart24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- 主内容区 -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 房间列表标题 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <ui:TextBlock Text="房间列表" FontTypography="BodyStrong"/>
                <ui:Button Content="" 
                          Command="{Binding RefreshRoomsCommand}" 
                          Appearance="Transparent"
                          Icon="{ui:SymbolIcon ArrowSync24}"
                          Margin="8,0,0,0"
                          ToolTip="刷新房间列表"/>
            </StackPanel>

            <!-- 房间列表 -->
            <ui:ListView Grid.Row="1" 
                        ItemsSource="{Binding Rooms}" 
                        SelectedItem="{Binding SelectedRoom}"
                        Margin="0,0,0,16">
                <ui:ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="8,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Grid.Column="0" Symbol="Video24" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Description}" FontSize="11" Opacity="0.5" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <ui:Badge Grid.Column="2" Content="{Binding Status}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ui:Button Grid.Column="3" 
                                      Appearance="Transparent"
                                      Icon="{ui:SymbolIcon Delete24}"
                                      ToolTip="删除房间"
                                      Command="{Binding DataContext.DeleteRoomCommand, RelativeSource={RelativeSource AncestorType=ui:ListView}}"
                                      CommandParameter="{Binding}"/>
                        </Grid>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>

            <!-- 创建房间 -->
            <StackPanel Grid.Row="2" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <ui:TextBox PlaceholderText="输入房间名称" 
                               Text="{Binding NewRoomName, UpdateSourceTrigger=PropertyChanged}" 
                               Width="280" Margin="0,0,8,0"/>
                    <ui:Button Content="创建房间" 
                              Command="{Binding CreateRoomCommand}" 
                              Icon="{ui:SymbolIcon Add24}"/>
                </StackPanel>
                <CheckBox Content="隐私模式" 
                         IsChecked="{Binding IsPrivacyMode}" 
                         ToolTip="{Binding PrivacyModeTooltip}"
                         Margin="0,0,0,0"/>
            </StackPanel>

            <!-- 获取推流地址按钮 -->
            <ui:Button Grid.Row="3" 
                      Content="获取推流地址" 
                      Command="{Binding OpenStreamingViewCommand}"
                      Appearance="Primary"
                      Icon="{ui:SymbolIcon Link24}"
                      HorizontalAlignment="Stretch"
                      Height="40"/>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="{ui:ThemeResource ControlFillColorDefaultBrush}" Padding="16,8">
            <Grid>
                <ui:TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                            HorizontalAlignment="Right" Width="100"/>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
